flask
transformers
torch
librosa
soundfile
imageio-ffmpeg
numpy
noisereduce
# PanPhon is REQUIRED for trusted scoring (the app validates that it can
# vectorize every assessable phoneme at startup; otherwise it runs in the
# untrusted fallback and does NOT update mastery). If PanPhon's `editdistance`
# dependency fails to build on your platform, install with:
#     pip install panphon --no-deps
#     pip install pyyaml regex munkres unicodecsv numpy
# The FeatureTable vector path this app uses does not need editdistance.
panphon
scipy
python-dotenv
cleanvoice-sdk>=2.0.5,<3

# Test runner (unit tests do not require torch/panphon; the acoustic layer is
# mocked/bypassed).
pytest

# Optional: only needed for the LLM-generation fallback in content.py.
# Talks to an LLM over an OpenAI-compatible endpoint; defaults to Google AI
# Studio (Gemini) -- set GEMINI_API_KEY to enable. The app runs fully
# offline/retrieval-only without it.
openai
